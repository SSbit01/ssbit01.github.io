---
import { Icon } from "astro-iconify";
---

<section id="home">
  <div id="intro">
    <h1>Hello World, I'm <a href="#about" id="me">Stefan Samson</a></h1>
    <h2 id="headline">An <mark>enthusiast</mark> coder of the <mark>cosmos</mark></h2>
    <a href="#about" id="scroll-down"><Icon name="icon-park-outline:double-down" /></a>
  </div>
</section>

<style lang="scss">
  #home {
    display: grid;
    animation: 40s ease infinite dynamic-bg;
    border-bottom: thin solid;
    background: #0a0f14 linear-gradient(135deg, #1e323c, #0a141e, #0a0f14, #14140a, #1e1e0f);
    background-size: 400% 400%;
    min-height: 100vh;
    color: #00fa9a;
  }

  #intro {
    transform-origin: top;
    font-size: clamp(0.9em, 5.2vmin, 1.5em);
    text-align: center;
    $px: 1.5rem;
    animation: 1.75s ease home;
    margin: auto;
    padding: 0 $px 5em $px;

    mark {
      position: relative;
      background: inherit;
      color: #7fffd4;
      font-style: italic;
      white-space: nowrap;
      &::after {
        position: absolute;
        right: 0;
        bottom: 0;
        left: 0;
        transform: scaleX(0);
        transform-origin: center;
        transition: transform 0.15s;
        border-radius: 2px;
        background: #48d1cc;
        height: 2px;
        content: "";
      }
      &:hover::after {
        transform: scaleX(1);
      }
    }
    > h1:first-child {
      font-size: clamp(2em, 10vmin, 2.5em);
      line-height: 1.25;
    }
  }

  #me {
    position: relative;
    color: #7fffd4;
    font-style: italic;
    text-decoration: underline double #afeeee;
    text-decoration-thickness: 2px;
    text-underline-offset: 4px;
    text-shadow: 0 0 3px;
    white-space: nowrap;
    &::after {
      position: absolute;
      transform: scaleX(0);
      transform-origin: right;
      transition:
        background 0.15s,
        transform 0.2s,
        box-shadow 0.2s;
      inset: 0;
      box-shadow: 0 0 8px #008080;
      border-radius: 4px;
      background: #66cdaa;
      content: "SSbit01";
      color: #000000;
      font-weight: 400;
    }
    &:hover::after {
      transform: scaleX(1);
      transform-origin: left;
    }
    &:active {
      user-select: none;
      &::after {
        box-shadow: 0 0 12px #66cdaa;
        background: #7fffd4;
      }
    }
  }

  #headline {
    color: #20b2aa;
    line-height: 1.25;
    &::after {
      display: inline-block;
      vertical-align: sub;
      animation: 1s infinite step-end caret;
      margin: 0 0.35em;
      box-shadow: 0 0 6px #e0ffff;
      border-radius: 2px;
      background: #e0ffff;
      width: 0.2em;
      height: 1em;
      content: "";
    }
  }

  #scroll-down {
    display: inline-block;
    filter: drop-shadow(0 0 3px #483d8b);
    animation: 1s infinite bounce;
    transition-duration: 0.2s;
    transition-property: color, filter, scale;
    margin-top: 0.5em;
    width: clamp(3em, 9vmin, 4em);
    color: #5f9ea0;
    &:hover {
      filter: drop-shadow(0 0 3px #008080);
      color: #66cdaa;
    }
    &:active {
      scale: 0.9;
      filter: drop-shadow(0 0 4px #20b2aa);
      color: #7fffd4;
    }
  }

  @keyframes dynamic-bg {
    50% {
      background-position: 100% 100%;
    }
  }

  @keyframes home {
    from,
    50% {
      opacity: 0;
      border-radius: 4em;
      background: #2e8b57;
      pointer-events: none;
    }
  }

  @keyframes caret {
    50% {
      opacity: 0;
    }
  }

  @keyframes bounce {
    50% {
      transform: translateY(20%);
    }
  }
</style>
